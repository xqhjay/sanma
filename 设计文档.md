# 奕码性能优化项目设计文档

## 1. 背景与目标

奕码是"大码乱序、小码音托"的字根双编码三码形码方案（拆分体系由 hanzi-chai 驱动）。
基于官方 1.2 版（四二顶-官方最新修正版）在字劫测评工具（chs.hertz.ltd，本地 CHS 源码 +
kc6000 字频）上的基线指标：

| 指标 | 基线值（官方 1.2） |
|---|---|
| 前 1500 二码字总数 | 563 |
| 前 1500 二码字加权比重 | 70.06% |
| 1501~3000 出简重码数 | 5 |
| 前 6000 出简重码总数 | 324 |
| 前 1500 出简重码数（硬约束=0） | 0 |
| 二码字总数（前 6000） | 676（26×26 全占满） |

**优化目标**：通过字根增删、归并调整与大码重排，改善上述四大指标。

**约束**：
1. 前 1500 高频字出简重码数 = 0（硬约束）
2. 字根归并组数 ≤ 320（当前 240 组）
3. 小码音托合规率高（保持或超过基线 93.9%）
4. 主根/副根归并规则：一组 = 1 主根 + 任意副根（同源/形似/变体），大小码共用
5. 保持"大码乱序、小码音托、三码形码、四二顶/三定双打字方式"方案骨架

## 2. 方案结构分析（已完成）

### 2.1 编码规则（从官方 YAML 逆向验证，100% 复现 8230 字全码）

- 每字拆分恒为 3 个字根元素（seq），每个元素按位置取大码（index=0）或小码（index=1）。
- 模式分布：`000`（三根大码）5486 字；`001`（末根小码，末两根相同）2268 字；`011`（280 字）。
- 编码 = `code(e1,i1)+code(e2,i2)+code(e3,i3)`；前两码恒为大码（音托只在末位起作用）。

### 2.2 出简重的决定机制（关键发现）

字劫对出简重的计算：`simpleCollision = 该字最短编码在码表行序中的位次`。
官方 1.2 码表组内行序经验证 635/645 组符合规则：

> **三码组内行序 = 非二简字按字频降序在前，二简字排最后**（官方用其内部频表，与 kc6000 有约 10 组轻微出入）

由此推出（设 top6000 内三码组 G，|G|≥2）：
- 二简字最短码为 2 码 → 永不计出简重（即使组内非首位）
- 组贡献出简重数 = `|G| - 1 - [G 含二简字]`
- **总出简重 = 6000 - 不同三码数 - 不同二码数（二码数固定 676 时 = 5324 - 三码数）**
- **前 1500 出简重=0 ⟺ 每个前 1500 字：是二简（前缀冠军），或为其三码组内频次最高的非二简字**

### 2.3 优化问题的组合本质

- 二码字（区间内）= 该区间内"前缀（首二码）频次冠军"的字数。
  前 1500 二码字 = |{前缀 P：P 的 kc6000 冠军 ∈ 前1500}|。
  基线 563 → 有 113 个前缀的冠军在 1501 名之后（高频字前缀聚集、低频字独占前缀）。
- 出简重总数 = 同三码聚集度（把 6000 字尽量散开到不同三码组）。
- 因此核心是**组合优化：把 240 个字根组的大码重新分配到 26 键**，使 top6000 字的
  （前缀, 全码）分布最大程度分散，同时保住前 1500 零出简重的硬约束。

## 3. 优化算法设计（参考 code_genie 的模拟退火框架）

### 3.1 变量与解空间

- **变量**：每个归并组 g 的大码 `K(g) ∈ 26 键`（乱序自由）；小码 `s(g)` 由音托优先固定。
- **合法性**：码位 `(K(g), s(g))` 全局唯一（同键同小码冲突即非法）。
- **初始解**：官方 1.2 布局（已满足硬约束，作为退火起点）。

### 3.2 快速评估器（内循环，无需生成码表/跑字劫）

预计算 top6000 字的 `seq`（元素+index 位），一次评估 = 6000 × 3 次查表 + 计数：
1. 每字三码 → 前缀 → 前缀冠军（按 kc6000 频序遍历一次即可）
2. 三码组内：冠军免出简重；二简免出简重；其余计出简重（按区间）
3. 输出：cd2_1500 / w2_1500 / sc_1500 / sc_1501_3000 / sc_total / 前缀覆盖数
复杂度 O(6000)，单次 <1ms，支撑百万级退火迭代。

### 3.3 目标函数

```
score = 3000·cd2_1500 + 40·w2_1500(%)
      - 5000·sc_1501_3000 - 300·sc_total
      - 200·(6000 中未覆盖前缀数)        // 二码位损失
      - 1e9·[sc_1500 > 0]                 // 硬约束
      （次要：键位负载方差惩罚，防止极端偏键）
```

### 3.4 邻域移动

- 单组移键：随机组 g → 随机新键 k（若 (k, s(g)) 占用则与占用组交换）
- 两组换键：K(g1) ↔ K(g2)
- 小码微调（受音托表约束的候选内调整，低概率）

### 3.5 流程

```
初始=官方布局 → 快速评估 → 循环（T 从 T0 几何降温至 T_min）：
  随机移动 → Δscore → Metropolis 接受/回滚 → 记录最优解
→ 最优布局 → 生成完整四二顶/三定码表 → 字劫本地测评复核四大指标
```

## 4. 字根增删策略（约束 3/4 的落实）

- **删**：使用频次极低的根组（top6000 内 0 次或 1 次出现）→ 合并入形似组作副根或直接删除（拆分系统自动改拆），释放组数额度。
- **增**（成字根，参考琉璃码）：高频整字（如"因/员/相"类）加为**新组主根或现有组副根**；
  额度 = 320 - 240 = 80 组。
- 每次增删 → 重跑 hanzi-chai 拆分+编码（build_splits.ts 已验证可 100% 复现官方）
  → 重新评估，仅采纳指标改善且拆分合理的变更。
- 归并组结构（主根+副根）保持官方体系，仅做增删微调。

## 5. 交付物

| 文件 | 说明 |
|---|---|
| `output/奕码优化版-四二顶.txt` | 四二顶码表（无一简，二简+全码） |
| `output/奕码优化版-三定.txt` | 三定码表（一简+二简+全码） |
| `output/奕码优化版.yaml` | 字根 YAML（hanzi-chai 格式，可直接驱动拆分） |
| `output/字根映射表.txt` | 组→大小码、主根/副根、音托规律标注 |
| `output/拆分表.txt` | top 字拆分+编码 |
| `output/字根图.pdf` | ChaiSans/diagram 风格字根键盘图 |
| `output/设计报告.md` | 指标对比、方法、变更清单 |

## 6. 验收标准（对比基线）

- 前 1500 二码字总数 > 563（目标 ≥ 580）
- 前 1500 二码加权比重 > 70.06%（目标 ≥ 73%）
- 1501~3000 出简重 ≤ 5
- 前 6000 出简重总数 < 324（目标 ≤ 300）
- 前 1500 出简重 = 0（必须）
- 归并组数 ≤ 320；音托合规率 ≥ 93.9%

## 7. 最终结果（F 方案，字劫实测验收）

| 指标 | 基线 | 目标 | 实测 | 结论 |
|---|---|---|---|---|
| 前 1500 二码字总数 | 563 | ≥580 | **655** | ✓ 超额 |
| 前 1500 二码加权比重 | 70.06% | ≥73% | **73.19%** | ✓ |
| 1501~3000 出简重 | 5 | ≤5 | **0** | ✓ 清零 |
| 前 6000 出简重总数 | 324 | <324 | **323** | ✓（贴近码位空间下界） |
| 前 1500 出简重 | 0 | =0 | **0** | ✓ |
| 归并组数 | 240 | ≤320 | **283**（+43 提升根） | ✓ |
| 音托合规率 | 93.9% | ≥93.9% | **94.6%** | ✓ |

优化手段：43 个高频副根提升为独立根（小码按自身读音音托）+ 240 基组大码 SA 全键盘重排。
所有交付物位于 `output/`，详见《output/设计报告.md》。
